# Alpha PR18 Precision Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply `ALPHA-PR18-FIX-001`, prove the corrected Architect skill behavior, and leave PR #18 ready for independent acceptance without changing runtime behavior.

**Architecture:** Keep `docs/ALPHA_GOVERNANCE.md` as the canonical Alpha board and governance source. Make a narrow documentation/skill correction across the existing governance surfaces, enforce the user-approved task-class vocabulary, and preserve every real-transport block while accurately describing dormant technical capability.

**Tech Stack:** Markdown governance/contracts, Codex skill Markdown, Pytest documentation guardrails, Git/GitHub.

**Spec:** `docs/ALPHA_GOVERNANCE.md`, plus the Owner approval attached to the active Codex goal.

## Global Constraints

- `REAL_TRANSPORT=BLOCKED`, `PRODUCTION=BLOCKED`, and `ADAPTER_RUN_REAL=BLOCKED`.
- Do not modify files under `app.py`, `core/`, `tools/`, frontend assets, or runtime configuration in this task.
- Do not call Oracle, APIs, Kafka, Jenkins, FTM, DNS, sockets, or subprocess-based product execution.
- Preserve `.codex-temp/` and `outputs/` exactly as pre-existing untracked user content.
- Task classes are exclusively `MECHANICAL`, `DEVELOPMENT`, `DEBUG`, `RESEARCH`, and `REVIEW`.
- Architect escalation is limited to material architecture, contract, policy, or risk deltas; routine Manager/Tester disagreement remains with the Manager.
- Existing manual executors may be technically capable of transport, but Alpha authorization remains absent and operational/contractual invocation remains blocked.
- `docs/ALPHA_GOVERNANCE.md` remains the canonical board; this plan is an execution artifact, not a competing source of truth.

---

### Task 1: Apply ALPHA-PR18-FIX-001

**Files:**
- Modify: `docs/ALPHA_GOVERNANCE.md`
- Modify: `.agents/skills/smartoffers-automation-architect/SKILL.md`
- Modify: `docs/ARCHITECTURE.md`
- Modify: `ai/real-execution/mvp7-8-3b-execution-plan.md`
- Modify: `tests/test_documentation_guardrails.py`

**Interfaces:**
- Consumes: the approved Alpha governance hierarchy, PR #18 independent-review findings, and the fixed `TASK_CLASS` taxonomy.
- Produces: one consistent governance contract for Manager/Tester escalation, dormant executors, operational release authority, and Alpha board routing.

- [ ] **Step 1: Record the RED behavior before editing the Architect skill**

Run a fresh-context review scenario against the current skill and record whether it mistakes an operational/contractual block for technical impossibility or routes a routine Manager/Tester dispute to Architect. The task implementer must read the saved baseline report before editing.

- [ ] **Step 2: Write the failing documentation guardrail expectations**

Update `tests/test_documentation_guardrails.py` first so it expects all of the following observable contract invariants:

```text
Architect escalation requires a material contract or risk divergence.
Alpha transport is operationally and contractually blocked.
Dormant manual executors are technically capable but not authorized.
Architect defines the risk envelope; an authorized operational role issues any future release.
TASK_CLASS uses only MECHANICAL|DEVELOPMENT|DEBUG|RESEARCH|REVIEW.
```

The previous assertion that all transport is “universally blocked” must fail until the skill wording is corrected.

- [ ] **Step 3: Run the focused test and verify RED**

Run:

```powershell
python -m pytest tests/test_documentation_guardrails.py -q
```

Expected: at least one assertion fails because the current Architect skill still claims universal blocking and the canonical board does not yet contain the fixed task-class routing.

- [ ] **Step 4: Apply the minimum governance and skill corrections**

Implement these exact outcomes:

```text
docs/ALPHA_GOVERNANCE.md
- restrict Manager/Tester -> Architect escalation to material contract or risk divergence;
- distinguish operational/contractual blocking from dormant technical capability;
- normalize the five TASK_CLASS values and assign RESEARCH/DEVELOPMENT/REVIEW to the approved cards/phases;
- move ALPHA-PR18-FIX-001 to REVIEW while the independent gate is pending;
- register the stale “Architect General issues EXECUTION_APPROVED” wording as reconciled historical authority text;
- retain CONTRACT_CONFLICT-001 as open until the separate DAG card is accepted.

.agents/skills/smartoffers-automation-architect/SKILL.md
- replace “universally blocked” with precise operational/contractual blocking;
- state that dormant entry points are not authorization and must not be invoked in Alpha;
- preserve all existing safety gates and role separation.

docs/ARCHITECTURE.md
- inventory tools/qa4_manual_smoke.py, tools/qa4_acm_manual_smoke.py,
  tools/qa4_bda_manual_smoke.py, and tools/qa4_api_health_smoke.py as dormant manual executors;
- state that their technical capability is not an Alpha release or kill-switch guarantee.

ai/real-execution/mvp7-8-3b-execution-plan.md
- remove Architect General as an autonomous operational approver;
- distinguish architectural envelope from a future explicit operational release;
- keep the plan EXECUTION_BLOCKED and planning-only.
```

- [ ] **Step 5: Run focused GREEN verification**

Run:

```powershell
python -m pytest tests/test_documentation_guardrails.py -q
```

Expected: all tests in the file pass.

- [ ] **Step 6: Validate the updated skill structure**

Run the bundled skill validator against:

```text
.agents/skills/smartoffers-automation-architect
```

Expected: valid frontmatter and no scaffold errors.

- [ ] **Step 7: Run the same pressure scenario with the corrected skill**

Expected behavior:

```text
Routine Manager/Tester disagreement -> Manager reconciliation.
Material contract/risk divergence -> Architect.
Dormant transport entry point -> technically capable, operationally unauthorized, do not invoke.
```

- [ ] **Step 8: Run repository verification**

Run:

```powershell
python -m pytest tests -q
git diff --check
git status --short --branch
```

Expected: the full suite passes, the diff check is clean, only the planned files plus this plan are changed, and `.codex-temp/`/`outputs/` remain untracked and unstaged.

- [ ] **Step 9: Commit the reviewed task**

Stage only the six planned paths and commit with:

```text
docs: refine Alpha governance precision
```

Do not push or mutate PR #18 until the independent Tester and Manager reconciliation approve the commit.
